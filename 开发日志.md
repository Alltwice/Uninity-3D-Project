# 规范说明

- `-[feat]-` (Feature) 新增功能或逻辑模块
- `-[refactor]-` (Refactor) 代码重构
- `-[perf]-` (Performance) 性能优化、手感调试、细节打磨
- `-[fix]-` (Bug Fix) 修复已知缺陷
- `-[chore]-` (Chore) 杂项、版本控制配置、资源管理等
- `-[docs]-` (Documentation) 文档更新
- `-[style]-` (Style) UI 表现、动画效果、代码格式美化
- ver1，ver2......代表同一天多次提交内容
---

# 2026/8/23

## ver1

### `-[chore]-`
- 最终技术路线替换完成，总体上在保证了RootMotion移动的视觉表现优秀的同时将角色的控制权完全交给了GamePlay，现回到main开发

---

# 2026/8/21

## ver2

### `-[fix]-`
- 给硬落地单独增加一个地面状态枚举，不再套用Idle地面状态，以解决重落地后移动虽锁，但是旋转未锁的问题
### `-[style]-`
- 现有的walk，run的mixer太死，无法胜任新架构中需要快速响应的要求，将其拆成了两个独立的动画片段，中间用fade完成过渡
- 恢复Dodge→Idle，FastRun→Idle的动画播放表现，两者再次走一套动画

## ver1

### `-[fix]-`
- 修复了烘焙动画到程序移动的间隔中存在的卡顿问题，确认为在新架构建立时handoffLoopState = animancer.Play(loop, 0f, FadeMode.FixedDuration);尝试仅使用handoff的程序化渐变效果控制代码过渡，但实际上忽略了动画间本身的衔接问题导致动画切换生硬，现删掉了程序控制渐变，动画渐变交由Animancer接管

---

# 2026/8/20

## ver2

### `-[fix]-`
- 修复了自新架构和技术路线确立依赖就长期存在的问题：0速度（原地）起跳时空中输入无效，重落地后输入无效，Dodge后输入无效，在多轮排查后发现是CC最小移动距离被设定为了0.001m作为保护，但是刚好撞上了前面这些在水平0输入为的情况下尝试加速的场景，此时速度不断从一个极小值累加，但是每次都在移动前被CC的保护给忽略，至于其余场景的输入有效的原因是动画位移是依赖动画采样点而非上一帧的移动数据，就算出现首帧被CC过滤的情况也会在后续被数值更大的动画位移数据给覆盖

## ver1

### `-[chore]-`
- 删减了失去消费的变量，枚举等，精简部分函数

---

# 2026/8/19

## ver2

### `-[fix]-`
- 修复角色起步时朝向起步帧方向持续移动的问题，确认为枚举语义错误导致仅在开始移动时刷新了一次输入意图，而在对应移动模式下无法正确的刷新每帧位置
- 修复转向时角色操作手感类似于转向角被卡死的情况，原技术路线中直接使用delateRotation，在距离目标角度x°后将旋转交接给程序完成，但是该问题几乎无法解决动画程序间的速度衔接问题，尤其是在起步转向时观感问题严重，且新路线使用SO配置好的枚举作为动画设置选用动画旋转角数据时虽然能解决转身，但是解决不了起步后的位移朝向和输入朝向不符的问题，虽然能通过运行时修改SO枚举设置做到，但是这样背离设计语义，最终选择了在转向时优先使用动画转向数据，判断玩家最终面向角和动画最终面向角的差值并用程序动态修正的解决思路，通过计算动画剩余时间播放比例*误差角度+需要旋转角度，得到了每帧需要被修正的角度，最终以到达目标角度修复该问题，做到及时响应 

## ver1

### `-[refactor]-`
- 新技术路线确立，进行了大规模的架构重构和新技术路线接入，具体情况如下
- 先前的版本中，将动画与移动的交互分为了纯代码驱动＋播放动画和在移动时实时读取deltaPosition和Rotation，通过动画播放来接管移动的两种做法，彼此通过在动画播放时通过枚举标签控制最终在motor中以何种方式移动。这种方式最终出现了职责模糊，gameplay需要依赖于美术资源，玩家控制受动画影响大，难以做到实时响应，以及最重要的在局内依赖Root motion会让最终构想的接入网络同步极其困难。因此最终选择了一条新的技术路线，仍然使用动画提供的数据，但是数据由离线烘焙产生，而不是局内实时计算。由此，程序拿到了数据，移动不再依附于动画，玩家响应更加快速，也为未来的网络同步接入铺路以免当前的系统愈加依附于RootMotion导致未来难以修改。
#### 新版技术路线架构的改动有几点
- 1：在原有动画预览窗口中依赖其模型资源和动画资源实现烘焙算法，并留有快速批量烘焙的函数接口
- 2：将原有Motor职责拆开，针对移动数据单独拉出了一层出来用于衔接动画于移动，处理移动数据，合成最终移动指令以及移动结束快照等
- 3：将状态机和动画控制器的职责拆的更轻，由一个中央模拟层负责高层职责的初始化于刷新，尤其统一调用移动更新，动画数据更新，并配有一个计划器用于匹配移动数据和动画数据确保动画的正确播放
#### 关于一些技术细节的论断如下
- 1：烘焙算法的实现：在设定好每秒烘焙时间后计算出每个采样点，然后在两个采样点之间的时间里计算原动画移动距离，旋转角度，以及单纯的位移长度存储到采样点数量大小个的数组中供读取使用
- 2：烘焙动画数据的使用：烘焙动画数据从点击UI按钮开始就会在某路径上创建一个空SO文件，之后开始执行bake算法，拿到bake内容，覆盖原先的SO数据，保存到磁盘上变成一个等待使用的SO文件，这里使用了PlayerMotionProfile保存数据，之后这份数据被裹挟在PlayerMotionDefinition，而后者则又新增了各类动画移动设定，是运行时真正需要的数据，再往上，数据被PlayerMotionCatalog包裹，打上ID，成为最终的被读取的索引用于planner组织动画数据
- 3：关于移动逻辑的实现：motion层的数据交错，最终需要一个composer组合器合成最终的移动命令，其中需要PlayerGameplayIntent提供输入方向，previousMotorResult上一帧的移动数据用于拿到移动速度，同时需要PlayerMotionFrame判断当前是否出于动画与纯速度驱动的混合状态以混合移动数据，最终计算出当前的移动旋转方式，需要移动的距离，面向的方向，下落速度，起跳速度等等，最终这份数据又被SimulationDriver组织，最终再motor丢到Simulate中开始执行真正的移动和动画更新
- 4：动画与移动的匹配：这里的动画已经关闭了自动更新演进，动画的播放更新完全依赖于定义的动画时间与实际动画播放时间的比例计算目前行进到了哪里，同样的，烘焙动画移动也依赖于这个比例，并通过维护这个比例最终传给动画层，动画依据比例进程更新

---

# 2026/8/15

## ver1

### `-[chore]-`
- 曾在8月12日确立的完全通过运行中的Root Motion来控制角色在过渡动画时的移动的方案不可行，
- 从架构层面来讲开始变成了动画驱动程序，玩家的物理移动不再成为唯一真实的控制来源，且该项目最终思考过实现网络同步，而依靠根运动难以接入网络

---

# 2026/8/14

## ver2

### `-[fix]-`
- 修复了各类起步动画到地面mixer后会出现明显卡顿的问题，最初以为是起步动画结束速度与mixer参数不匹配，检查后发现是动画在结尾存在不完整残留帧，而这部分虽然通常不满一帧的运动，但是其数据还是会被计算：移动路程/每帧时间由此得到了一个极低的数据导致在那一瞬间会出现速度骤降。通过给起步动画和mixer之间专门做了一个fade在接近动画结尾时通过提高mixer的比重而削减掉残留帧带来的影响，并最终在动画被mixer完全接管后转为代码驱动

## ver1

### `-[perf]-`
- 为了确保转身动画不会滑步同时保证对玩家的即时响应现将转向规则做了修改，将动画驱动的动画继续拆为是否启用旋转和移动，通过枚举组合与位运算检测移动旋转开启情况，以下为具体规则
1：转向动画触发条件是在当角色面向角度和输入角度发生150°（非死编码）差距后触发转向动画，此时锁定输入移动和旋转
2：触发期间当角色面向角度和输入意图的角度小于120°时解锁输入旋转，以减少滑步的同时应对摄像机方位改变/输入角度改变，让角色灵活响应
3：当出现零输入或是输入意图与最终旋转角度差距过大时停止转向动画，此时视为玩家有新的意图而非继续转身
以上规则极大改善了转向手感，但是仍存在若干问题，最终细节，逻辑，规则仍需调整


---

# 2026/8/13

## ver1

### `-[fix]-`
- 对空中落地后检测是否有输入特殊化处理，如果有则打断轻落地动画避免滑步
- 将walk和run放到了一组mixer中，以便补上两者移动时无切换过渡动画的问题

### `-[feat]-`
- 在AI帮助下建立了初版的动画预览器，用于在脱离代码环境下检查模型和动画的适配情况


---

# 2026/8/12

## ver2

### `-[fix]-`
- 新增一个是否启用旋转的表示，用于修复在启用根动画但是不读取输入以及旋转后造成的动画播放过程中的硬直问题


## ver1

### `-[feat]-`
- 新增一个脚本专门用于读取和提供动画根运动数据，主要用于处理一些不好用代码控制的过渡动画效果，运动数据最终传给motor脚本中的某函数，并由UnityOnAnimatorMove方法自动调用移动效果，至于是代码驱动还是动画驱动效果新增枚举区分，在动画播放阶段就被动画层确定


---

# 2026/8/11

## ver2

### `-[refactor]-`
- 终于意识到了维护motor子状态机，维护dodge生命周期，以及和主状态机和动画层间的纠葛的架构设计是多么的智障了
- 拆掉了所有的无用枚举和数据快照以及多余状态机，将状态类继续拆细，控制权统一交给主状态机，数据快照只保留主状态机的状态记录以及切换原因，包括Dodge每帧移动特性存在的原因保留数据快照供使用，完全拆掉了中间繁杂冗余抽象无用的中间层，同时大大减少动画与状态机间的解耦，动画层目前仅和controller起交互，在切换状态时捕捉数据快照并依赖数据快照播放特殊状态动画，过渡动画以及循环动画，因为少了一大堆中间数据和子状态机数据，现在同样将动画数据处理类拆掉，动画控制层目前只需要读取状态机数据快照即可运转

### `-[chore]-`
- 仍在继续收集动画资源在进行动画调优
- 虽然目前仍存在若干问题，但是解决了卡住很多天的问题，整体架构清爽爆了，理解和review难度也大大降低

## ver1

### `-[feat]-`
- 新增Dodge能力，将其接入Context和输入缓冲系统中，并将原先疾跑状态替换为闪避后实现，疾跑由枚举管理其生命周期，并同样通过枚举记录切换原因，输入意图等确保能够协调各个动画间衔接部分的正确播放

### `-[refactor]-`
- 为了实现各部分的衔接动画（idle→walk，walk_in,walk_out等）重写了Motor能力脚本，由枚举记录移动模式切换原因，记录前后移动状态以便日后正确播放移动衔接动画，也就是说目前的移动已经成为了一个存在idle，run，fastrun，dodge，air组成的小型的子状态机

---

# 2026/8/8

## ver2 

### `-[chore]-`
- 更新了AGENTS.md主要内容为规范AI生成代码格式
- 将除动画外所有晚间数据配置为SO文件方便后续配置调整
- 再次更换了动画包体，还未接入测试

## ver1

### `-[fix]-`
- 修复了重落地动画在无输入时也会被打断的问题

### `-[chore]-`
- 对昨天状态机重构结果做了彻底的review，并对代码格式做了若干调整

---
# 2026/8/7

## ver2

### `-[fix]-`
- 随着判断条件的增加和部分组件行为对时序的严格要求，单一的刷新+状态切换无法满足当前要求，多出出现了状态判断失效失误的问题，最终原因导向都是那一帧之差的行为意图和状态切换矛盾，因此再次优化结构，将一个状态控制器内的update中修改为更新前，状态中和更新后三部分，依据不同状态类的具体要求实现不同状态切换函数以严格管控数据时序问题，同时新建统一的状态意图处理类用于处理交错复杂的的判断并返回唯一的状态切换参考标准，并同时创建状态意图记录类用于记录先前当前之后的数据以确保对于某些过渡状态和动画有一个确切的参考值

## ver1

### `-[fix]-`
- 修复了在奔跑状态下的滑步问题，原因是加减速时原有方法无法处理大角度移动时的速度骤变问题，通过点乘计算角度当检测到目标方向和当前方向存在大角度时提高加速度以解决了“滑冰”的问题

### `-[chore]-`
- 接入新的测试场景
- 接入新的完整动画包

---
# 2026/8/5

## ver2

### `-[feat]-`

- 新增疾跑到停止间动画


## ver1

### `-[fix]-`

- 通过启用重落地动画在y轴的Bake Into Pose解决了在空中播放动画的问题
- 删去了Controll中重复判断播放重落地动画播放的部分，当前重落地动画全权交由具体空中状态类判断并发送请求

### `-[chore]-`

- 纯白模场景在测试移动时难以找到参照，导入了一套场景美术素材作为后续测试开发场地

---

# 2026/7/31

## ver1

### `-[feat]-`

- 加入了空中待机动画，修改空中动画逻辑为起跳后进入空中状态，等待起跳动画完整播放后进入空中待机动画，如果已经在空中且当前并未触发起跳动画说明直接坠落，进入空中待机动画
- 加入重落地动画以及对应状态类，状态锁为动画播放进度以实现高处落地硬直，重落地判定通过获取落地前速度实现，若超过设定值则判定为一次重落地

### `-[fix]-`

- 修复了下斜坡移动时频繁进入空中状态的问题，原因是当玩家高速移动时原先所施加的重力无法稳定将玩家压在斜面上，通过重写地面检测脚本通过检测与地面距离使得在0.3m的高度下时刻计算玩家和地面之间的距离并通过向下移动将玩家压在地面上，同时兼顾了检测地面法线和角色夹角判断坡度，坡度过大不吸附等功能，同时通过记录玩家之前和之后的地面状态判断玩家是处于地面，起跳，还是落地从而避免误判

### `-[chore]-`
- 将开发日志格式修改为md

---

# 2026/7/30

## ver2

### `-[chore]-`

- 更新了AGENT.md，旨在解决AI编写代码常常出现的过分防御性编程而使代码可读性差，结构冗余，掩盖真实问题等

### `-[feat]-`

- 新增了走路，快跑的两种移动状态以及对应移动动画，同时新增对应状态以及切换按键逻辑，同时为了更好的管理移动状态，现将各个原有速度变量统一删除交由枚举控制管理并由一个变量保存实施切换

## ver1

### `-[refactor]-`

- 完整review了Animacer版本的动画控制部分，清除了其中过分防御性编程的部分

### `-[style]-`

- 修改了LinearMixer中Parameter参数的提供方式，由SmoothDamp替换为了直接由归一化的移动速度提供，避免了二次平滑（在速度处理部分做了平滑因此取消动画层的平滑），使视觉效果更加贴合实际速度

---

# 2026/7/29

## ver1

### `-[refactor]-`

- 为了避免双状态机职责不清和未来可能的动画管理混乱现引入Animancer替换Animator
- 将原来的动画控制脚本现在拆成三个独立部分，将角色数据接收处理后转交给动画参数和控制脚本，而具体播放什么动画逻辑参照角色状态机，由具体状态类发送动画切换请求

---

# 2026/7/27

## ver1

### `-[refactor]-`

- 重构玩家状态机，将状态切换逻辑剥离下放到具体状态类中，并由一个方法统一包裹状态锁和状态逻辑，但满足切换要求时向Controller发出请求，由Controller完成切换操作
- Controller中新增一个泛型切换状态函数，用于接收泛型状态类型，以解决泛型函数无法接收运行时确定数据类型的问题，同时将原泛型状态切换函数的逻辑拆到非泛型中，保留泛型函数用于将数据转为非泛型函数的参数，而切换逻辑统一走非泛型切换函数

---

# 2026/7/23

## ver1

### `-[chore]-`

- 重新开始开发流程，暂时搁置模型动画问题，预计留有模型动画替换接口以便后续替换
- 使用了mixao的动画包作为当前版本最终模型动画效果
- 尝试重新梳理和优化架构
- 尝试接入UnityMCP提效

---

# 2026/6/2

## ver1

### `-[chore]-`

- 在CSDN找到了一份有关跳跃的解决方案
- 接入了Animancer
- 切换了更合适的模型
- 找不到合适的动画方案

---

# 2026/5/31

## ver1

### `-[chore]-`

- 跳跃动画不合适，逻辑层和动画层冲突严重，动画违和感强，且当前的Animator已经出现连线繁杂问题，尝试测试和寻求解决办法

---

# 2026/5/28

## ver1

### `-[fix]-`

- 确认无法跳跃的原因是跳跃指令先于跳跃行为被触发，导致跳跃逻辑未正常触发，此时是否未陆地的判断也出错，程序误以为角色在空中导致重力持续累积，修复方式是添加了输入缓冲，确保在缓冲时间内跳跃一定会被触发而不是顶掉

### `-[feat]-`

- 按此思路增加了输入缓冲脚本，通过字典/枚举/输入缓冲结构体维护缓冲命令，通过主动依赖注入建立和输入读取器的联系，且通过接口被赋予输入缓冲逻辑
- 落地了玩家跳起后提前检测地面以播放落地动画的功能，但是实测发现效果不好，考虑从动画入手

---

# 2026/5/27

## ver2

### `-[chore]-`

- 尝试给当前CC足部接入射线检测用于提前判断落地动画播放
- 发现打包后出现角色无法跳跃且重力持续性累积的问题

## ver1

### `-[chore]-`

- 进行了各种动画素材的拼接取舍理解（3C在表现上的打磨要比代码难啊......）
- 下一阶段关注问题：
  1. 动画可打断问题
  2. 角色控制器的胶囊判定不准确问题
  3. 代码驱动动画逻辑问题

---

# 2026/5/26

## ver1

### `-[fix]-`

- 确认动画扭曲为导入动画包时缺少模型导致Avatar无法正确绑定导致

---

# 2026/5/25

## ver1

### `-[feat]-`

- 代码驱动动画脚本编写完成，将数据归一化后传入animator处理

### `-[chore]-`

- 发现原MMD模型在导入Unity后骨骼绑定有问题，rigidbodies / joints等无法被有效识别且会被干扰，尝试在建模软件解决问题

---

# 2026/5/24

## ver2

### `-[chore]-`

- 接入待机动画，尝试使用Blend Tree+代码驱动的方式驱动动画
- 下一步待办
  1. 项目层级，代码文件分类整理
  2. 系统学习模型/动画导入，Blend Tree
  3. 完成代码驱动动画脚本以及基本动画接入

## ver1

### `-[chore]-`

- 接入了外部模型

### `-[fix]-`

- 修复了困扰我很久的摄像机拉高后位置不居中问题，发现是Cinemachine数据未清空导致

---

# 2026/5/23

## ver1

### `-[feat]-`

- 优化当前速度控制，现在所有的速度均来自地面移动，起步以及其余状态均使用MoveTowards模拟加速/减速度状态，使移动均有过渡态，空中速度优先继承自地面，解决了空中减速严重问题

---

# 2026/5/22

## ver2

### `-[feat]-`

- 加入跳跃能力
- 加入空中移动状态类

### `-[chore]-`

- 当前存在只要进入空中就会用空中的移动速度而无视在地面加速度的问题
- 下阶段需要搭建测试场景，已经能跑能跳，可引入模型动画

## ver1

### `-[feat]-`

- 搭建好了状态机并将先前移动逻辑拆入
- 精简了结构删去了数据存储和提供类，不为了过度解耦导致逻辑复杂度和抽象程度过高
- 将摄像机模式从疑似非常老式的跟随角色旋转变为了角色移动和摄像机移动分离，因此也解决了5/20 ver2中的问题
- 编写了摄像机移动逻辑，依靠依赖注入读取输入而非依赖单例和硬引用
- 摄像机会随着抬高而增加视野范围让视觉更舒适

---

# 2026/5/21

## ver1

### `-[feat]-`

- 预感到继续将移动逻辑写入一个脚本会让逻辑混乱，在此提前构建状态机
- 继续迭代重构上次状态机设计，更改点如下：
  1. 继续细化状态机，将其拆分为状态基类+状态切换管理类+数据存放处理类+具体状态类
  2. 数据存放处理类中又继续分出了一个Context的玩家移动数据包概念，通过数据存放处理类实时提供玩家移动数据，并由其持有引用，具体状态类无需在构造函数中重复写入引用逻辑
  3. 状态切换管理类将原先的枚举切换状态改为了泛型＋字典处理，无需维护额外枚举，泛用性更高，同时提供了一个状态切换表管理状态切换
  4. 总体解决了上个项目中状态机控制类过于庞杂的问题，具体状态类需要在构造函数里获取大量其余部分引用数据的问题，解决了原先状态切换判断逻辑散落，各个状态类在自己的Update中处理切换逻辑不便于扩展和管理的问题
- 当前实际完成了状态机管理，状态基类，数据存放管理和具体状态类待构建

---

# 2026/5/20

## ver2

### `-[feat]-`

- 成功接入依赖注入层，接入Cinemachine和第三人称跟随，起码让角色动起来了

### `-[chore]-`

- 问题记录
  1. 当前角色原地旋转会围绕摄像机转圈而不是绕自己中轴，推测问题是移动朝向和摄像机位置绑定相关导致
  2. 当前角色按S后退会伴随摄像机以及角色抽搐，推测是角色移动会尝试旋转到移动方向，但是角色移动方向又会修正这一次旋转的结果
  3. 原地旋转速度和移动中旋转速度完全不一致，可能需要分离

## ver1

### `-[chore]-`

- 搭建了最基本的场景白模，处理了一些层级和文件夹创建的杂项内容

### `-[feat]-`

- 彻底拆解重构了上个项目的输入控制器并接入了新项目，修改部分如下
  1. 取消了原有的单例设计大量静态事件，改为接口+具体脚本依赖注入＋胶水层的设计减少隐式耦合
  2. 输入读取器不再会获取任何脚本的引用减少耦合
  3. 计划创建多个Action和加入事件订阅和取消的设计可处理不同场景下的输入和禁用输入使各个脚本职责更清晰便于管理
