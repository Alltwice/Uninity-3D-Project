# 修改与交付规则

## 防御性编程

编写或修改代码时，避免为了暂时消除报错而机械加入大量判空、日志、提前返回或运行时兜底。

遇到问题时，应优先分析根本原因，并从依赖关系、初始化流程、生命周期、配置或职责划分等方面解决，而不是仅隐藏错误表现。

必要的防御性处理可以保留，但应有明确目的。不要用防御性代码掩盖错误配置或不合理的项目结构，也不要在清理防御性代码时误删正常的业务判断。

## 代码格式

代码格式应整体保持紧凑、连续，避免为了视觉分隔而在类或函数内部频繁插入空行。

对于较长的函数调用、条件表达式或语句，不要仅因行长度而机械换行或增加多层缩进；在不影响可读性的前提下，优先保持单行。

同时应尽量保持现有项目的代码风格，避免产生与当前修改无关的格式调整。

## 修饰符使用

编写类、结构体、字段或变量时，不要默认添加 `sealed`、`readonly` 等限制性修饰符。

只有在当前设计确实需要禁止继承、禁止重新赋值，或需要明确表达稳定不变量时，才使用对应修饰符。不要仅因为“更安全”“更严格”或“未来可能有用”而机械添加，也不要在修改无关代码时顺手增加这些限制。

## 注释维护

修改已有函数、参数、字段、枚举、结构体或类的职责与语义时，应同步检查并调整对应注释，使其准确反映当前实现。

如果原有注释已经因行为或含义变化而失效，应修改或删除，不要原封不动地保留、复制或照搬旧注释。注释应优先描述当前职责、约束和设计原因，避免留下与实际代码不一致的说明。

## 完成报告

每次任务结束后，请说明：

1. 修改了哪些文件。
2. 每个文件修改的具体行号或行号范围。
3. 修改了哪一部分内容。
4. 被修改部分原本或当前负责的功能职责。
5. 如涉及防御性代码，简要说明保留、删除或调整的原因。